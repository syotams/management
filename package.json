{
  "name": "management",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "db:push": "cd backend && npx prisma db push && npx prisma generate"
  }
}
